{
  "name": "my-first-react-stack",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "start": "node server/server.js"
  }
}